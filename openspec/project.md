# Project

Minimal macOS utility written in Rust to keep a small window always on top and let the user select a file. The initial scope is intentionally small and focused on core behavior (select a file, pin window on top), with room to extend (preview, per-type behaviors) later.

