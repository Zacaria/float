1. Default setting: set `fit_window` to true when no persisted setting exists
2. Auto-fit on file selection if `fit_window` is true (clamp to screen)
3. Keep manual Fit Now action for on-demand resize
4. Persist user preference when toggled
5. Validate macOS and Windows: select file → auto-fit occurs

